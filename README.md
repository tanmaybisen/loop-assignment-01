#### Loop Backend Engineer - Assignment

#### Create Virtual Env
python -m venv venv

#### To Run in local (env) git-bash cmd
source ./nenv/Scripts/activate

#### To Run FastAPI app on localhost
uvicorn main:app --reload